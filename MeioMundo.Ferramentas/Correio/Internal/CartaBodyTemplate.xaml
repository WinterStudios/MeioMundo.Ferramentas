<UserControl x:Class="MeioMundo.Ferramentas.Correio.Internal.CartaBodyTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeioMundo.Ferramentas.Correio.Internal"
             xmlns:internal="clr-namespace:MeioMundo.Ferramentas.Internal"
             xmlns:main="clr-namespace:MeioMundo.Ferramentas"
             mc:Ignorable="d" 
             Height="29.7cm" Width="21cm"
             Background="White"
             Foreground="Black"
             >

    <UserControl.Resources>
        <internal:ConvertDoubleToDoubleNegative x:Key="ConvertDoubleToNegative"/>
        <SolidColorBrush x:Key="Remetente.Foreground" Color="#007BE1"/>
        <Style x:Key="EmpresaSide" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segui UI"/>
        </Style>

    </UserControl.Resources>

    <Border Padding="0.5cm">
        <Grid>
            <Border HorizontalAlignment="Left" VerticalAlignment="Stretch">
                <Grid RenderTransformOrigin="0.5,0">
                    <Grid.RenderTransform>
                        <RotateTransform Angle="-90"/>
                    </Grid.RenderTransform>
                    <TextBlock x:Name="leftSideText" HorizontalAlignment="Center" FontSize="8pt" FontWeight="Light" FontFamily="Calibri"/>
                </Grid>
            </Border>

            <Grid Margin="0.75cm">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!--Zona da Cabeça de Carta | o primeiro terço da primeira folha-->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4.0cm" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="1.5cm" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2.0cm"/>
                        <ColumnDefinition Width="11.0cm"/>
                        <ColumnDefinition Width="1.5cm"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
                        <Image HorizontalAlignment="Left" VerticalAlignment="Top" Source="/MeioMundo.Ferramentas;component/Assets/carta-logo.png" Width="5cm" RenderOptions.EdgeMode="Aliased" RenderOptions.BitmapScalingMode="HighQuality" />
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.DominaçãoSocial}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformationMorada.Rua}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformationMorada.CodigoPostal}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformationMorada.Pais}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.NIF}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.CapitalSocial}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.Telefone}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.Email}}"/>
                        <TextBlock Style="{DynamicResource EmpresaSide}" Text="{Binding Source={x:Static main:MeioMundoInformation.WebPage}}"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="2">
                    <Border x:Name="richboxTools">
                        <Border.RenderTransform>
                            <TranslateTransform X="{Binding ElementName=richboxTools, Path=ActualHeight, Converter={StaticResource ConvertDoubleToNegative}}" />
                        </Border.RenderTransform>
                        <StackPanel>

                        </StackPanel>
                    </Border>

                    <RichTextBox>
                        <FlowDocument>
                            <Paragraph>
                                <Bold>Example Text</Bold>
                            </Paragraph>
                        </FlowDocument>
                    </RichTextBox>
                </Grid>
            </Grid>
        </Grid>
    </Border>

</UserControl>
